matrix:
  include:
    - language: java
      jdk: openjdk8
      before_script:
        - cd Robot
      script:
        - ./gradlew build

    - language: rust
      cache: cargo
      rust: stable
      before_script:
        - cd Vision
      script:
        - cargo test
